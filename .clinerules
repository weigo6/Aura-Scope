# AuraScope 项目开发指南 (STM32F103C8T6)

你现在是 AuraScope 示波器项目的资深嵌入式工程师。请严格遵守以下硬件配置与开发规范。

## 1. 核心硬件参数
- **MCU**: STM32F103C8T6 (64KB Flash, 20KB SRAM)
- **内核**: ARM Cortex-M3 (72MHz)
- **库**: STM32Cube HAL 库

## 2. 引脚分配表 (Pinout)
### 显示屏 (1.8" TFT, ST7735, 128x160)
- **SCL (SCK)**: PA5
- **SDA (MOSI)**: PA7
- **RES (Reset)**: PB5
- **DC (Data/Command)**: PB6
- **CS (Chip Select)**: PB7
- **BLK (Backlight)**: PB8

### 信号采集 (示波器核心)
- **ADC 输入**: PA3 (ADC1), PA4 (ADC2)
- **采集模式**: 同步采样模式 (Dual Regular Simultaneous Mode)
- **波形输出**: PA2 (PWM 输出，用于自检信号)

### 频率检测逻辑 (Hysteresis Comparator)
- **原理**: 模拟信号经滞回比较器转为方波，通过定时器捕获测量周期。
- **CH1 (PA3) 频率捕获**: 对应引脚 **PA6** (使用 TIM3_CH1)
- **CH2 (PA4) 频率捕获**: 对应引脚 **PA1** (使用 TIM2_CH2)

### 交互与输入
- **按键 (Active Low)**: 
  - KEY1: PB13 | KEY2: PB14 | KEY3: PB15 | KEY4: PA8
- **EC11 旋转编码器**:
  - A脚: PB4 | B脚: PB3 | D脚(按键): PB9 | (C/E脚已接地)
- **指示灯 (Active Low)**:
  - LED1: PC14 | LED2: PC15

### 通讯接口
- **USB**: PA11/PA12 (USB CDC 虚拟串口)
- **UART1**: PA9 (TX), PA10 (RX)
- **UART3**: PB10 (TX), PB11 (RX)

## 3. 代码生成规范
- **DMA处理**: ADC采样与串口数据发送等能采用 DMA 的场景必须使用 DMA，以减轻 CPU 负担。
- **UI 库**: 针对 ST7735 驱动，优先使用硬件 SPI 逻辑。
- **低功耗/性能**: 禁用不必要的浮点运算，优先使用定点数处理波形数据。
- **命名**: 变量采用小驼峰 (e.g., adcBuffer)，硬件宏定义采用大写 (e.g., LED1_GPIO_PORT)。
- **注释**: 关键算法和硬件配置必须提供中文注释。

## 4. 关键逻辑约束
- 项目结构：这是一个 Cmake 工程，涉及板载驱动（例如 ST7735 驱动）在 Hardware/，核心业务逻辑在 Core/Src 和 Core/Inc。
- 禁用编译与构建: 不要直接使用 `cmake` 或 `make` 命令，因为该项目依赖 STM32 插件生成的 CMakePresets。禁用编译，因为你无法直接操作。
- 硬件配置：本项目基于 STM32CubeMX，主要的初始化代码在 main.c 和 stm32xxxx_hal_msp.c。
- 代码风格：使用 HAL 库风格。在修改代码时，必须将逻辑写在 /* USER CODE BEGIN */ 和 /* USER CODE END */ 标签之间，以防 CubeMX 重新生成代码时覆盖。
- 查找逻辑：当找不到函数定义时，优先搜索 Drivers/STM32xxxx_HAL_Driver/ 目录。
- 任务流：修改完代码后，请提示我检查引脚配置是否需要同步在 .ioc 文件中（虽然你无法修改 .ioc 图形界面）。

## 5. 上位机（PC）程序
- 项目具有一个基于 Pyside6 的上位机程序，位于 AuraScope-Viewer/aurascope.py ，使用 UV 作为 Python 包管理器，请使用 uv run 运行Python程序。

## 6. 轨道信号分析仪-上位机程序
为了使目前的示波器项目更贴合“轨道交通”专业，打算在现有的设计基础上，针对轨道电路增加一些特定分析功能。
该上位机程序，位于 AuraScope-Viewer/tracksignal.py 处。
以下实现细节均通过 USB CDC 回传至 PC 后进行数据处理，处理结果显示在上位机界面中：

### A. 移频信号包络解调分析
*   专业背景：ZPW-2000A轨道电路通过移频（FSK）方式传输信号。如果波形发生畸变或频率偏移，会导致列车无法正常接收码位（如L、LU、U码）。
*   功能实现：在现有的软件算法中，增加一个“轨道信号模式”。利用ADC采集到的数据，通过数字检波算法提取出低频调制信号（10.3Hz-29Hz），并在屏幕上显示该低频包络。这可以帮助维修人员判断“码信号”是否稳定。

### B. 25Hz相敏轨道电路相位测量
*   专业背景：在老旧车站或特定区段仍使用25Hz相敏轨道电路。它的特点是需要比较“轨道电压”和“局部电压”的相位差。
*   功能实现：利用STM32双通道ADC，测量两路25Hz正弦波的时间差。在UI界面上增加一个“相位差显示”功能，直接给出夹角数值（如相位差是否在90°左右），这是轨道交通信号特有的测量指标。

### C. 信号电源杂波与谐波分析
*   专业背景：轨道信号设备对电源质量要求极高，电力机车的牵引电流会对信号系统产生电磁干扰。
*   功能实现：利用已有的数据，增加简单的FFT（快速傅里叶变换）功能，专门用于分析信号线中是否存在50Hz工频干扰及其谐波分量。这能帮助判断轨道电路的“不平衡电流”是否超标。